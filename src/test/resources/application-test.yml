datasources:
  default:
    url: jdbc:h2:mem:testDb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driverClassName: org.h2.Driver

# habilita os logs de debug da transaction para troubleshooting
#logger:
#  levels:
#    io.micronaut.transaction: DEBUG